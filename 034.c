#include <stdio.h>
#include <stdlib.h>


int main()
{
    int *pi = calloc(5,sizeof(int));

    //int *pi = malloc(5 * sizeof(int));
    //memset(pi, 0, 5* sizeof(int));

    system("PAUSE");
    return 0;
}
